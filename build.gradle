buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:4.2.1"
        classpath "com.google.gms:google-services:${System.getenv("VERSION")}"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
